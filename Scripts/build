#!/bin/bash -e
echo $0
source "$(cd $(dirname ${BASH_SOURCE[0]}) && pwd)/consts"

# build project
msbuild /target:Clean /property:Configuration=Release /verbosity:minimal $SOLUTION
msbuild /property:Configuration=Release /verbosity:minimal $SOLUTION
